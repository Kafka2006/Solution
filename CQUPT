Q6019.(语言: C)用穷举法，输出2——n之间所有的素数。
输入格式：
 printf("please input number n:\n");
 scanf("%d",&n);

输出格式：

"%d\t"
没有使用题目所给的完全穷举，缩小了搜索区间。

题解：

```
#include <stdio.h>

int main(){
    int n;
    printf("please input number n:\n");
    scanf("%d",&n);
    for(int j=2;j<=n;j++){
    	int t=1;
        for(int i=2;i*i<=j;i++){//显然n的因数要么在$(1,\sqrt(n))$，要么在$(\sqrt(n)+1,n)$
        	if(j%i==0&&j!=2) t=0;
        }
        if(t) printf("%d\t",j);
    }
    return 0;
}
```
